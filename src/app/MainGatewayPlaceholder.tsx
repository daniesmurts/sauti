import React from 'react';
import {RootNavigator} from './RootNavigator';

/**
 * Placeholder entry point for the authenticated main app.
 * Will be replaced by the real main navigator (conversations + chat screens).
 */
export function MainGatewayPlaceholder(): React.JSX.Element {
  return <RootNavigator />;
}
